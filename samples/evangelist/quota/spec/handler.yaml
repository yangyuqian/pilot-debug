apiVersion: "config.istio.io/v1alpha2"
kind: memquota
metadata:
  name: handler
  namespace: istio-system
spec:
  quotas:
  - name: requestcount.quota.istio-system
    maxAmount: 20
    validDuration: 30s
    overrides:
    - dimensions:
        destination: mock1-http1.soa-test.svc.cluster.local
      maxAmount: 10
      validDuration: 30s
